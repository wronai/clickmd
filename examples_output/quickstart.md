## To jest nagłówek
Zwykły tekst bez markdown - bez renderowania
Pogrubiony tekst z *kursywą*
✅ Operacja zakończona pomyślnie!
⚠️  Uwaga: dysk prawie pełny
🛑 Błąd: nie można połączyć z serwerem
ℹ️  Przetwarzanie 42 plików...

## Przykład kodu Python


```python
def hello(name: str) -> str:
    return f"Hello, {name}!"

print(hello("World"))
```


```
┌─────────┬────────┬───────────┐
│Nazwa    │Wersja  │Status     │
├─────────┼────────┼───────────┤
│clickmd  │1.5.0   │✅ aktywny  │
│Python   │3.12    │✅ aktywny  │
└─────────┴────────┴───────────┘
```
```
┌── ℹ️ Info ───────────────────────────────────────────────────────────────┐
│ clickmd to zero-dependency biblioteka                                    │
│ do renderowania markdown w terminalu.                                    │
└──────────────────────────────────────────────────────────────────────────┘
```
